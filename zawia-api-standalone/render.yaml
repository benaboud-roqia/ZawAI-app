services:
  - type: web
    name: zawia-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: AI_INTEGRATIONS_ANTHROPIC_API_KEY
        sync: false
